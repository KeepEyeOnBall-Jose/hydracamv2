# GitHub Copilot Instructions

This repository uses `AGENTS.md` as the canonical agent control plane.

When working in this repo, follow the root `AGENTS.md` for project context,
commands, validation policy, Git hygiene, and architecture notes. Keep this file
as a short adapter so Copilot and other agents do not drift from the shared
instructions.
