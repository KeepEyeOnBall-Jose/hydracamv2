# CLAUDE.md

This repository uses `AGENTS.md` as the canonical agent control plane.

Before making changes, read and follow `AGENTS.md` from the repository root. Keep
Claude-specific guidance here only when it cannot be represented in `AGENTS.md`.
